kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: kind-charts-cluster
nodes:
- role: control-plane
  extraMounts:
  - hostPath: .devcontainer/zscaler.pem
    containerPath: /etc/ssl/certs/zscaler.pem
